export const ADDON_ID = 'storybook/msw-addon'
export const PANEL_ID = `${ADDON_ID}/panel`
export const PARAM_KEY = `msw`

export const EVENTS = {
  SEND: `${ADDON_ID}/send`,
  UPDATE: `${ADDON_ID}/update`,
  UPDATE_RESPONSES: `${ADDON_ID}/update-responses`,
  RESET: `${ADDON_ID}/reset`,
}
