{
  "private": true,
  "name": "msw-storybook-addon",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspace msw-storybook-docs sb:start",
    "build": "yarn workspace msw-storybook-addon build && yarn workspace msw-storybook-docs sb:build"
  }
}
